md-toolbar.md-padding.lightBlue(layout='row' layout-align='start end')
  md-button(ng-href='/')
    h2 EMEETER
    
div.md-padding
  h2.md-title Iniciar Sesión
  div(layout="row", layout-margin)
    div(flex)
      h2.md-title
        md-icon(md-font-icon='material-icons') person
        | Datos de la cuenta

      form(
        name='form',
        role='form',
        ng-submit='form.$valid && submit()',
        novalidate)
        
        md-input-container.form-group
          span.input-group-addon
            md-icon(md-font-icon='material-icons') 
          input.form-control(
            placeholder='Ingresa tu email',
            ng-model='form.email',
            type='email',
            required)
            
        md-input-container.form-group
          span.input-group-addon
            md-icon(md-font-icon='material-icons') 
          input.form-control(
            placeholder='Ingresa tu clave',
            ng-model='form.password',
            ng-minlength='4',
            type='password',
            required)
            
        div(layout="row")
          md-button(ng-href='/users/signup') No tengo cuenta
          
          md-button(ng-href='/users/recover') Olvidé mi clave
          
          md-button(
            ng-disabled='form.$invalid || submitting',
            type='submit')
            md-icon(md-font-icon='material-icons')
            |  Sign In

    div(flex)
      h2.md-title
        md-icon(md-font-icon='material-icons') person
        | Texto de ejemplo
