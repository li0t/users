md-toolbar.md-padding.lightBlue(layout='row' layout-align='start end')
  div
    h2 {{ session.group.profile.name }} : TAREAS : COLABORADORES

md-content
  div(layout='row')
    md-button.md-fab.md-warn(ng-href='/groups/{{session.group._id}}/tasks/{{task}}/collaborators')
      md-icon(md-font-icon='material-icons') chevron_left
           
  div(ng-show='fetching && !members')        
    md-icon(md-font-icon='material-icons') sync
    | Recopilando Información
      
  div(ng-show='!fetching && members')
    h3 Collaboradores
      
    md-button.md-raised(ng-repeat='collaborator in collaborators.list')
      | {{ collaborator.user.email }}
      md-icon(md-font-icon='material-icons', ng-click='collaborators.remove($index)') close

    md-input-container
      md-select(
        ng-change='collaborators.add()',
        ng-model='form.collaborator'
        placeholder='Agrega colaboradores')
        
        md-option(
          ng-repeat='member in members',
          value='{{ $index }}')
          | {{ member.user.email }}
    
    div(layout='row')
      md-button(ng-href='/groups/{{session.group._id}}/tasks/{{task}}/collaborators')
        md-icon(md-font-icon='material-icons') chevron_left
        | Cancelar

      md-button(
        ng-disabled='!collaborators.list.length',
        ng-click='submit()')
        | Listo
        md-icon(md-font-icon='material-icons') check
